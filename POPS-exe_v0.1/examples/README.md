# Example manifests

`group.tsv` and `control.tsv` demonstrate the accepted schema only. The FASTQ
paths are placeholders and are not bundled with the repository.

For a real run, replace the paths with preprocessed FASTQs and ensure that every
path is visible inside the Docker container when using Docker.
